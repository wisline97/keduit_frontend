<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <pre>
        var arr1 = [0,10,20,30,40]
        var arr2 = [10,20,30,40,0]

        var a = 90
        var b = 60

        [문제1] arr1 배열의 값들을 앞으로 한칸씩 당기고 맨뒤에 a의  값을 저장한다.
        [정답1] arr1 = [10,20,30,40,90];

         
        [문제2] arr2 배열의 값들을 뒤로 하나씩 밀어낸후 맨 앞에 b의 값을 저장한다.
        [정답2] arr2 = [60,10,20,30,40];

        
    </pre>
    <script>
      var arr1 = [0, 10, 20, 30, 40];
      var arr2 = [10, 20, 30, 40, 0];

      var a = 90;
      var b = 60;

      for (i = 0; i < arr1.length - 1; i++) {
        arr1[i] = arr1[i + 1];
      }
      arr1[arr1.length - 1] = a;

      for (i = arr2.length - 1; i > 0; i--) {
        arr2[i] = arr2[i - 1];
      }
      arr1[arr1.length - 1] = a;
      arr2[0] = b;
      document.write("<h3>" + "정답1 " + "[" + arr1 + "]" + "</h3>");
      document.write("<h3>" + "정답2 " + "[" + arr2 + "]" + "</h3>");
      document.write("<br/>");
    </script>
    <a href="main"> 메인화면으로 </a>
  </body>
</html>
