<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <pre>
        [문제]
            1. 10회 반복을 한다.
            2. 1~100 사이의 랜덤 숫자를 저장한다.(학생의 성적)
            3. 성적이 60점 이상이면 합격생이다.
            ---------------------------------------
            [1] 전교생(10명)의 총점과 평균을 출력한다.
            [2] 합격자 수를 출력한다.
            [3] 1등 학생의 번호와 성적을 출력한다.

    </pre>
    <script>
      var i = 0;
      var arr1 = [];

      while (true) {
        if (i == 10) {
          break;
        }
        var score = parseInt(Math.random() * 100) + 1;
        arr1[i] = score;
        if (arr1[i] >= 60) {
          document.write(i + 1, "번 학생", arr1[i], "점 합격" + "<br/>");
        } else {
          document.write(i + 1, "번 학생", arr1[i], "점 불합격" + "<br/>");
        }
        document.write("===========" + "<br/>");
        i++;
      }

      var count = 0;
      var max = 0;
      var maxNum = 0;
      var total = 0;

      for (i = 0; i < arr1.length; i++) {
        total += arr1[i];

        if (arr1[i] >= 60) {
          count += 1;
        }

        if (arr1[i] > max) {
          max = arr1[i];
          maxNum = i + 1;
        }
      }

      document.write(
        "<h2>" +
          "[1] 총점과 평균: " +
          total +
          " 점, " +
          total / 10 +
          " 점" +
          "</h2>"
      );
      document.write("<h2>" + "[2] 합격자 수: " + count + " 명" + "</h2>");
      document.write(
        "<h2>" + "[3] 1등 학생의 번호: " + maxNum + " 번" + "</h2>"
      );
      document.write("<h2>" + "[3] 1등 학생의 성적: " + max + " 점" + "</h2>");
    </script>
    <a href="main"> 메인화면으로 </a>
  </body>
</html>
