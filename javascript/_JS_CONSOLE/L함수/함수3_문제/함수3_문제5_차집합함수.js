/*
	arr1 배열과  arr2 배열을 매개변수로 받아서 
	서로 겹치지않는 값만 배열로 만들어서 반환해주는 함수를 만드시오.
 */

function differenceArr(arr1, arr2) {
	var arr = [];
	return arr;
}

var arr1 = [12, 32, 33, 43, 65];
var arr2 = [21, 12, 43, 2, 4, 5];
var arr = differenceArr(arr1, arr2);
console.log(arr);