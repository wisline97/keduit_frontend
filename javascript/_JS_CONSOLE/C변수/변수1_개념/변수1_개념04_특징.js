/*
	[1] 변수의 특징	
		(1) 변수는 값을 한개만 저장할 수 있다.
 		(2) 새로운 값이 저장되면 이전의 값은 사라진다.
		(3) 새로 변수를 선언하면 기존의 변수는 사라진다.
 */

var num = 9;
console.log(num);

num = 3;
console.log(num);

var num = 10; // var 로 생성한 num 은 사라진다.
console.log(num);
